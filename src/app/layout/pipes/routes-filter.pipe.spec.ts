import { RoutesFilterPipe } from './routes-filter.pipe';

describe('RoutesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RoutesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
